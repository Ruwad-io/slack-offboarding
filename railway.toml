[build]
builder = "DOCKERFILE"
watchPatterns = ["src/**", "templates/**", "static/**", "pyproject.toml", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
